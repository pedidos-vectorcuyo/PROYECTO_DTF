# Este workflow fue desactivado — el deploy se realiza desde deploy.yml
# Se mantiene el archivo para no romper referencias existentes.
name: Deploy to Deploy Branch (DISABLED)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Manually triggered only'
        required: false

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo 'Este workflow está desactivado. Usar deploy.yml.'
